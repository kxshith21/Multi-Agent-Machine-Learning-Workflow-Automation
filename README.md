# AgentML — Multi-Agent ML Experiment Orchestrator
#
# Usage:
#   python -m pytest tests/ -v          # run all tests
#   python -m src.orchestrator.graph    # run end-to-end with sample data
#
# See Memory.md for current phase status.
