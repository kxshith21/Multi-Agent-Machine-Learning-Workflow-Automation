# AgentML — Multi-Agent ML Experiment Orchestrator
# Python 3.10+

# Core pipeline
langgraph>=1.0.0
langchain-core>=0.3.0
groq>=0.9.0
python-dotenv>=1.0.0

# Data & ML
pandas>=2.0.0
scikit-learn>=1.3.0
xgboost>=2.0.0
numpy>=1.24.0

# Report templating
jinja2>=3.1.0

# UI (Phase 7+)
streamlit>=1.35.0

# Testing
pytest>=8.0.0
pytest-cov>=4.0.0

# NOT ALLOWED (see Rules.md §3):
# torch, tensorflow, keras — deep learning out of scope
# optuna, hyperopt       — HPO out of scope
# mlflow                 — experiment tracking out of scope
# asyncio (for concurrency) — use ThreadPoolExecutor instead
